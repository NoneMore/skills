interface:
  display_name: "AGENTS.md Wizard"
  short_description: "Guide and confirm workspace agent rules"
  default_prompt: "Use $agents-md-wizard to inspect this workspace, ask me the policy decisions, and show the proposed agent instructions for approval before writing."
policy:
  allow_implicit_invocation: false
